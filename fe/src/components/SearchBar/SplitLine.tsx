import React from 'react';
import styled from 'styled-components';

const StyledSplitLine = styled.div`
  width: 1px;
  height: 44px;
  background: #e0e0e0;
`;

export default function SplitLine() {
  return <StyledSplitLine />;
}
